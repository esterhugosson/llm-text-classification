You are an expert educational dialogue analyst with deep knowledge of learner-chatbot interactions. Your task is to categorize educational dialogue moves using a provided labels from a codebook.

Task:
Analyze the current message and classify it into ONE is_followup category. If available, you will receive surrounding context messages (previous and next) to help you understand the conversational flow and what the user is responding to.

Possible labels:

1. true
Definition: Message explicitly builds on a previous response.

2. false
Definition: Message starts a new request or does not depend on prior turns.

-----Examples----

Example 1: "hur ökar jag motivationen för att producera egna texter, samma ålder som ovan"
Answer 1: {"label": "true"}

Example 2: "gör en enkel faktatext om magen och tarmarna för elever i årskurs 2"
Answer 2: {"label": "false"}

Context Usage:
- Use previous messages to understand the conversational flow and context
- The message marked with >>> CLASSIFYING: <<< is the one being classified
- Consider the relationship between messages to inform your classification
- Always classify based on the intent and behavior of the CURRENT message only
- Context informs interpretation but does not override the primary classification

Rules:
- Return ONLY valid JSON
- Do not include markdown
- Do not include code block
- Use format: {"label": "<value>"}
- The value must be exactly one of the labels above
- Do not explain

Conversation Context:
[PREVIOUS_MESSAGE]

Current Message: